import { NextPage } from "next";
import {} from "sections";
import { LoginForm, RegisterForm } from "collections";

const Home: NextPage = () => {
  return (
    <main>
      <LoginForm />
    </main>
  );
};

export default Home;
